{% extends 'default.html.twig' %}

{% set hasBreadcrumb = true %}

{% set metaDescription = 'Program Jízdy králů v Doloplazích ' ~ kingsRide.year ~ '.' %}

{% block title %}{{ 'Program'|trans }} — {{ 'The Kings\' Ride in Doloplazy'|transchoice(1) }} {{ kingsRide.year }}{% endblock %}

{% block breadcrumb %}
<span class="breadcrumb-item active">{{ 'The Kings\' Ride in Doloplazy'|transchoice(1) }} {{ kingsRide.year }}</span>
<span class="breadcrumb-item active">{{ 'Program'|trans }}</span>
{% endblock %}

{% block content %}
<div class="container">
    <h2 class="mb-3">{{ 'Program'|trans }}</h2>
    <h3>{{ 'Feast party'|trans }}<br><small>{{ kingsRide.saturdayDate|localizeddate('none', 'none', app.request.locale, null, 'EEEE d. MMMM YYYY' ) }}</small></h3>
    <div class="mb-4">
        {{ kingsRide.saturdayProgram|raw }}
    </div>
    <h3>{{ 'The Kings\' Ride'|transchoice(1) }}<br><small>{{ kingsRide.sundayDate|localizeddate('none', 'none', app.request.locale, null, 'EEEE d. MMMM YYYY' ) }}</small></h3>
    {{ kingsRide.sundayProgram|raw }}
</div>
{% endblock %}
